//#region guest-js/index.d.ts
type ColorScheme = {
  primary?: string;
  onPrimary?: string;
  primaryContainer?: string;
  onPrimaryContainer?: string;
  inversePrimary?: string;
  secondary?: string;
  onSecondary?: string;
  secondaryContainer?: string;
  onSecondaryContainer?: string;
  tertiary?: string;
  onTertiary?: string;
  tertiaryContainer?: string;
  onTertiaryContainer?: string;
  background?: string;
  onBackground?: string;
  surface?: string;
  onSurface?: string;
  surfaceVariant?: string;
  onSurfaceVariant?: string;
  inverseSurface?: string;
  inverseOnSurface?: string;
  outline?: string;
};
type InsetsScheme = {
  rawInsetTop?: number;
  rawInsetBottom?: number;
  rawInsetLeft?: number;
  rawInsetRight?: number;
  adjustedInsetTop?: number;
  adjustedInsetBottom?: number;
  adjustedInsetLeft?: number;
  adjustedInsetRight?: number;
  scaleFactor?: number;
};
type BarColorScheme = {
  color?: string;
};
/**
 * Main `tauri-plugin-m3` Utility Class
 */
declare class M3 {
  /**
   * Get all inset properties for proper Edge-To-Edge Display
   * @example
   * ```javascript
   * import { M3 } from "tauri-plugin-m3";
   *
   * let insets = await M3.getInsets();
   * ```
   * @return A InsetsScheme object or false if unsuccessful
   */
  static getInsets(): Promise<InsetsScheme | false>;
  /**
   * Set the current status and navigation bar color
   * @example
   * ```javascript
   * import { M3 } from "tauri-plugin-m3";
   *
   * // either "dark", "light" or "system"
   * // default is "system"
   * let isSuccess = await M3.setBarColor("dark");
   * ```
   * @return A boolean indicating if successful or not
   */
  static setBarColor(color?: "dark" | "light" | "system"): Promise<Boolean>;
  /**
   * Return the fetched ColorScheme
   * @example
   * ```javascript
   * import { M3 } from "tauri-plugin-m3";
   *
   * // either "dark", "light" or "system"
   * // default is "system"
   * let colorScheme = await M3.getColors("dark");
   * ```
   * @return A ColorScheme object or false if unsuccessful
   */
  static getColors(theme?: "dark" | "light" | "system"): Promise<ColorScheme | false>;
  /**
   * Apply all colors in ColorScheme as CSS environment variables
   * @example
   * ```javascript
   * import { M3 } from "tauri-plugin-m3";
   *
   * // either "dark", "light" or "system"
   * // default is "system"
   * let isSuccess = await M3.applyColors("dark");
   * ```
   * @return A boolean indicating if successful or not
   */
  static applyColors(theme?: "dark" | "light" | "system"): Promise<Boolean>;
}
//#endregion
export { BarColorScheme, ColorScheme, InsetsScheme, M3 };